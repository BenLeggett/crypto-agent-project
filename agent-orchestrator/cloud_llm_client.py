from __future__ import annotations


def complete(
    prompt: str,
    model: str,
    api_key: str | None = None,
    provider: str | None = None,
) -> str:
    """Placeholder cloud completion call."""
    _ = (prompt, model, api_key, provider)
    return ""
